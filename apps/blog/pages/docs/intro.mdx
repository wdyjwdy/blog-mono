import { CreateTable } from '../../components'

export function DataJS() {
  return [
    {
      key: 'Object',
      values: [
        [['Creating', 'Creating an Object'], ['Accessing', 'Accessing an Object'], ['Modifying', 'Modifying an Object'], ['Checking', 'Checking Object Props'], ['Enumerating', 'Enumerating an Object'], ['Copying', 'Copying an Object'], 'Prototype Methods'],
      ]
    },
    {
      key: 'Array',
      values: [
        [['Creating', 'Creating an Array'], ['Accessing', 'Accessing an Array'], ['Modifying', 'Modifying an Array'], ['Iterating', 'Iterating an Array'], ['Transforming', 'Transforming an Array'], ['Searching', 'Searching an Array'], ['Sorting', 'Sorting an Array'], ['Enumerating', 'Enumerating an Array'], ['Copying', 'Copying an Array'], ['Checking', 'Checking an Array']]
      ]
    },
    {
      key: 'Function',
      values: [
        ['call', 'apply', 'bind'],
      ]
    },
    {
      key: 'String',
      values: [
        ['at', 'charAt', 'indexOf', 'lastIndexOf'],
        ['startsWith', 'endsWith', 'includes'],
        ['slice', 'substring'],
        ['trim', 'trimStart', 'trimEnd', 'padStart', 'padEnd'],
        ['toUpperCase', 'toLowerCase'],
        ['replace', 'replaceAll', 'match', 'matchAll', 'search'],
        ['split', 'repeat', 'concat']
      ]
    },
    {
      key: 'Promise',
      values: [
        ['Promise', 'withResolvers'],
        ['all', 'any', 'race', 'allSettled'],
        ['resolve', 'reject'],
        ['then', 'catch', 'finally']
      ]
    },
  ]
}

export const tsData = [
  {
    key: 'TS',
    values: [
      ['Types', 'Type Inference']
    ]
  }
]

export const dataConfig = [
  {
    key: 'JSON',
    values: [
      [["Basic Types", "basic-types"], ["Collection Types", "collection-types"]]
    ],
  },
  {
    key: 'YAML',
    values: [
      [["Basic Types", "basic-types-1"], ["Collection Types", "collection-types-1"]]
    ],
  }
]

## JS
<CreateTable data={DataJS()} path='docs/js' />
## TS
<CreateTable data={tsData} path='docs/ts' singlePage={true} />
## Config Files
<CreateTable data={dataConfig} path='docs/config-files' singlePage={true} />